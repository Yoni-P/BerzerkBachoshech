using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * An enum of different kind of borders in the maze
 */
public enum Border
{
    UNDEFINED,
    OUTER_WALL,
    INNER_WALL,
    GATE,
    EXIT,
    OPENING
}
